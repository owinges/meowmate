// import { ADD_POOP } from './entryActions';
import moment from 'moment';

const initialState = {
    entries: [
        {
            date: moment([2019, 2, 23]),
            data: [
                {
                    // time: moment([2019, 2, 23]),
                    // consistency: 'dry' || 'normal' || 'runny'
                }
            ]
        }
    ]
}